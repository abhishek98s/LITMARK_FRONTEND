export interface folder {
    img: string
    title: string,
}

export interface Chip {
    name: string,
    active: boolean
}

export interface Recentbookmark {
    category: string,
    title: string,
    date: string,
    img: string,
    link: string
}